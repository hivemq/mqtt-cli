# Gradle
.gradle
build/

# IntelliJ
out/
*.iml
.idea/*
!.idea/codeStyles
!.idea/inspectionProfiles
!.idea/runConfigurations

# Ignore Kotlin compiler and error files in plugins/.kotlin
mqtt-cli-plugins/.kotlin/

.java-version
.DS_Store

# Claude Code local state — never part of a release commit.
# Scoped deliberately: .claude/skills and .claude/settings.json are shared team
# config and must stay committable.
.claude/worktrees/
.claude/settings.local.json
